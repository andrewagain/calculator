name: Deploy react project
on:
  push:
    branches:
      - "master"
  pull_request:
    branches:
      - "master"



jobs:
  build:
    name: Build phase
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        run: |
          pwd
          ls -l 
               